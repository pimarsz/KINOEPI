{% extends 'base.html.twig' %}

{% block title %}{{ 'title.screening_delete'|trans({'%id%': screening.screening_id}) }}{% endblock %}

{% block body %}
    <h1>{{ 'title.screening_delete'|trans({'%id%': screening.screening_id}) }}</h1>
    {{ form_start(form, { method: 'post', action: url('screening_delete', {'id': screening.screening_id}) }) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-success" />
    {{ form_end(form) }}
    <a href="{{ url('screening_show') }}" title="{{ 'label.back_to_list'|trans }}" class="btn btn-default">
        <span class="fa fa-mail-reply" aria-hidden="true"></span>
        {{ 'label.back_to_list'|trans }}
    </a>
{% endblock %}